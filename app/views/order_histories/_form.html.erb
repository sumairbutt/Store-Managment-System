<%= form_with(model: order_history, class: "contents") do |form| %>
  <% if order_history.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(order_history.errors.count, "error") %> prohibited this order_history from being saved:</h2>

      <ul>
        <% order_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
